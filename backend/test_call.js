const jwt = require("jsonwebtoken"); const { PrismaClient } = require("@prisma/client"); const p = new PrismaClient(); async function run() { const doc = await p.user.findFirst({where: {email: "dr.shah@hospital.com"}}); const token = jwt.sign({ id: doc.id, role: doc.role }, "hospital_queue_super_secret_key_2024", { expiresIn: "24h" }); const res = await fetch("http://localhost:3000/api/queues/call-next", { method: "POST", headers: { "Authorization": "Bearer " + token }}); console.log(await res.json()); p.$disconnect(); } run();
